# Intentionally empty.
#
# Passed as --secrets.files by run.py (demo mode). Streamlit loads ONLY the
# files named on that flag, so pointing it here replaces .streamlit/secrets.toml
# and its [auth] block: the sign-in gate is off for the offline demo without
# moving, renaming or editing the real secrets file.
